{
  "name": "obsn-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for the OBS notification overlay: a mock OBS producer, and a second implementation of the wire format and configuration schema that is checked against the C++ one.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "mock": "tsx src/cli.ts mock"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
